Lecture Space Structures
$$
\delta u_{0,\alpha}: -N_{\alpha\beta / \beta} = \hat{n_{\alpha}} \newline
$$
$$
\delta \psi_{\alpha}:-M_{\alpha\beta /\beta} - Q_{\alpha} = 0
$$
$$
\delta \omega_{0}:-Q_{\alpha/\alpha} + p = 0
$$
Let's expand it
$$
-N_{\alpha\beta / \beta} = \hat{n_{\alpha}}
$$
for $\alpha = 1$
$$
-N_{11 / 1} -N_{11 / 2} = \hat{n_{1}}
$$
for $\alpha = 2$

$$
-N_{22 / 1} -N_{22 / 2} = \hat{n_{2}}
$$
(Insert picture diagram of equilibrium of a plate for normal forces)

Equilibrium along the x axis:

$$
(N_{11} + dN_{11})dx_2 - N_{11}dx_2+(N_{12}dN_{12})dx_1-N_{12}dx_1 + \hat{n}_1dx_1dx_2 = 0
$$
$$
\Rightarrow N_{11/1}+N_{12/2} + \hat{n}_1 = 0
$$
Equilibrium along y axis:

$$
N_{22/2}+N_{21/1} + \hat{n}_1 = 0
$$
Constitutive Law: Plane Stress [[Constitutive Equations]] [[Stress Tensor]]

The approximation of plane stresses is done by the fact that $\sigma_{33}$ is close to 0. This doesn't mean that the plate can work only as a membrane, because forces distributed or concentreaded can be supported by the plane after an initial bend. This bend allows the plane to support the forces globally insted for the thickness of the plate itself (remember the rubber example), since the stress in the z direction will be distributed in the x and y axes.

$$
\sigma_{33} \approx 0, \varepsilon_{33} \neq 0
$$

$$
\begin{Bmatrix}
\sigma_{11} \\
\sigma_{22} \\
\sigma_{12}
\end{Bmatrix}  = \frac{E}{1-\gamma^2} \begin{bmatrix} 
1 & \gamma & 0 & 0 \\
\gamma & 1 & 0 & 0 \\

\end{bmatrix} \begin{Bmatrix} \end{Bmatrix}
$$
---
$$
\{ N \} = [A]\{\xi\}
$$
$$
\{M\} = [D] \{k\}
$$
$$
\{ Q\} = [A]^s\{t\}
$$

Weak form (Mindlin Elastic Plate)
$$
\delta W_i = \delta W_e
$$
$$
\int_A \delta \{\xi\}^T[A]\{\xi\}+\delta \{k\}^T[D]\{k\}+\delta \{t\}^T[A]^s\{t\}dA
$$
With unknowns
$$
\{\xi\} = \begin{Bmatrix}
\xi_{11} \\
\xi_{22} \\
\xi_{12} + 
\end{Bmatrix} = \{\xi\} = \begin{Bmatrix}
\xi_{11} \\
\xi_{22} \\
\xi_{12} + 
\end{Bmatrix}
$$
$$
\{\xi\} = \begin{Bmatrix}
\sigma_{11} \\
\sigma_{22} \\
\sigma_{12}
\end{Bmatrix}
$$
$$
\{t\} = \begin{Bmatrix}
t_{1} \\
t_{2} \\
\end{Bmatrix} = \begin{Bmatrix}
\xi_{0/2} + \psi_2\\
w_{0/1} + \psi_1 \\
\end{Bmatrix}
$$

# Kirchhoff Plate

Starting from Mindlin we can simplify the expressions (if the plate is thin)

Models are valid if the ration between length and thickness are at least 100 times or higher. If it's between 50 and 100 works fine if the metal is isotropic, but if it's a composite material one should check. Lower than that this model stops being valid.

Generalized deformations

$$
\varepsilon_{\alpha \beta} = \frac{1}{2}(u_{0\alpha/\beta}+u_{0\beta/\alpha}) - x_3w_{0/\alpha \beta} = \xi_{\alpha\beta} + x_3 k_{\alpha\beta}
$$
$$
\gamma_{\alpha3} = 0

$$
with $\xi_{\alpha \beta} = \frac{1}{2}(u_{0\alpha/\beta}+u_{0\beta/\alpha})$ and $K_{\alpha \beta} = -w_{0/\alpha \beta}$ 

Generalized stresses

we use the principle of virtual displacements

$$
\Rightarrow = \int_A (\delta u_0 I_0 \ddot{u}_0 + \delta v_0 I_0 \ddot{v}_0 + \delta w_0 I_0 \ddot{w}_0)dA
$$
with $I_0 = \int\rho zdz$ for homogeneous material considering a reference surface as the middle one and negligible rotatory (due to studying only low frequencies responses)